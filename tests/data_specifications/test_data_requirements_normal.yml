# データと加工手順の事前定義

metadata:
  purpose: 解析目的
  note:
    - 用語:
        - 用語1: 説明
        - 用語2: 説明
    - 注意点:
        - 注意点1
        - 注意点2

target:
  - single_data_X
  - ts_data_Y

data:
  rawA:
    description: 概要
    format: フォーマット
    unit: 単位
    special_note: |
      md形式による特記事項
      改行も可能
  rawB:
    description: 概要
    format: フォーマット
    unit: 単位
    special_note: |
      md形式による特記事項
      改行も可能
  dataA:
    description: 概要
    format: フォーマット
    unit: 単位
    required_data:
      - rawA
    required_parameter:
      - parameter1
    process:
      - 処理1
      - 処理2
    special_note: |
      md形式による特記事項
      改行も可能
  dataB:
    description: 概要
    format: フォーマット
    unit: 単位
    required_data:
      - rawB
      - dataA
    process:
      - 処理1
      - 処理2
    special_note: |
      md形式による特記事項
      改行も可能
  single_data_X:
    description: 概要
    format: フォーマット
    unit: 単位
    required_data:
      - dataA
      - dataB
    required_parameter:
      - parameter2
    process:
      - 処理1
      - 処理2
  ts_data_Y:
    description: 概要
    format: フォーマット
    unit: 単位
    required_data:
      - dataA
      - dataB
    process:
      - 処理1
      - 処理2

parameter:
  parameter1:
    description: 概要
    format: フォーマット
    unit: 単位
    value: 0
    special_note: |
      md形式による特記事項
      改行も可能
  parameter2:
    description: 概要
    format: フォーマット
    unit: 単位
    value: 0
    special_note: |
      md形式による特記事項
      改行も可能
